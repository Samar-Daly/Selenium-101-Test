# Enable parallel execution of tests
junit.jupiter.execution.parallel.enabled = true

# Configure parallelism for tests
junit.jupiter.execution.parallel.mode.default = concurrent
junit.jupiter.execution.parallel.mode.classes = concurrent
junit.jupiter.execution.parallel.config.strategy = fixed
junit.jupiter.execution.parallel.config.fixed.parallelism = 2  # Number of threads for parallel execution
